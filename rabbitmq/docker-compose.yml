version: "2"
services: 
  rabbitmq38:
    image: rabbitmq:3.8
    ports: 
      - 15672:15672
      - 5672:5672
    volumes: 
      - ./data:/var/lib/rabbitmq/mnesia/
    environment: 
      RABBITMQ_DEFAULT_USER: root
      RABBITMQ_DEFAULT_PASS: root
    restart: always
    privileged: true
